<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tuTurno.app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.tuTurno.app.LoginActivity"
            android:screenOrientation="portrait"
            android:label="tuTurno">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name="com.tuTurno.app.RecuperoActivity"
            android:screenOrientation="portrait"
            android:label="Recuperar Contraseña" />
        <activity
            android:name="com.tuTurno.app.RegistroActivity"
            android:screenOrientation="portrait"
            android:label="Registrar Cliente" />
        <activity
            android:name="com.tuTurno.app.LaFuria.ActividadPrincipal"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name="com.tuTurno.app.LaFuria.ActividadAdmin"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name="com.tuTurno.app.ActividadAdministrador"
            android:screenOrientation="portrait">
        </activity>

        <!-- Crop Imagenes-->
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8540442919500654~6266197352" />

        <service
            android:name=".ServicioMensajeriaFirebase"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_baseline_notifications_active_24" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/secondaryLightColor" />

        <!-- registering the receiver -->
        <receiver
            android:name=".MiAlarma"
            android:enabled="true"
            android:exported="true"
            android:permission="TODO"
            tools:ignore="ExportedReceiver">
        </receiver>
    </application>

</manifest>