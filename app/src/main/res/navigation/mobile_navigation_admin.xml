<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation_admin"
    app:startDestination="@+id/fragment_admin">

    <fragment
        android:id="@+id/fragment_admin"
        android:name="com.tuTurno.app.LaFuria.ui.ListaTurnos.ListAdmin"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_listar_admin">
    </fragment>

    <fragment
        android:id="@+id/fragment_contacto_admin"
        android:name="com.tuTurno.app.LaFuria.FragmentContacto_admin"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_contacto_admin">
    </fragment>

    <fragment
        android:id="@+id/acercade"
        android:name="com.tuTurno.app.LaFuria.AcercadeAdmin"
        android:label="@string/app_name"
        tools:layout="@layout/acercadeadmin">
    </fragment>
    <fragment
        android:id="@+id/AdminCodigo"
        android:name="com.tuTurno.app.LaFuria.fragmentsConfig.AdminPager"
        android:label="@string/app_name"
        tools:layout="@layout/admincodigo">
    </fragment>

</navigation>